% function setCurrentChannels(this, main, low, high)
function setCurrentChannels(this, main, low, high)
    this.currentChannels = [main, low, high];

    %this.currentChannels.main = main;
    %this.currentChannels.low = low;
    %this.currentChannels.high = high;
end